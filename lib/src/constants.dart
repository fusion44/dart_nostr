import 'models/models.dart';

const defaultLoggerName = 'dn';

const defaultDatabaseName = 'nostr.db';

const defaultSchemas = [
  ContactSchema,
  Nip05Schema,
  Nip19KeySetSchema,
  ProfileSchema,
  RelaySchema,
];

const defaultIdToken = '{{ID}}';
